apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: yelli-ingress
  namespace: prisma
spec:
  tls:
    - secretName: tlssecret-tls
  rules:
    - http:
        paths:
          - path: /*
            backend:
              serviceName: yelli
              servicePort: 443
          - path: /graphql
            backend:
              serviceName: yelli-api
              servicePort: 443
          - path: /vscode
            backend:
              serviceName: code-server
              servicePort: 8443
