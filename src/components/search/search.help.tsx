import React from 'react'

import { Root } from './search.help.css'

const Help: React.FC = () => (
  <Root>
    Press <kbd>enter</kbd> to select, <kbd>↑↓</kbd> to
    navigate, <kbd>esc</kbd> to dismiss
  </Root>
)

export default Help
