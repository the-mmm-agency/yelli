import { styled } from '@material-ui/styles'
import { Button as MuiButton } from '@material-ui/core'
import { button } from 'util/system'

const Button = styled(MuiButton)(button)
export default Button
